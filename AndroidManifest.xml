<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="mx.xperience.gamespace"
          android:versionCode="1"
          android:versionName="1.0"
          coreApp="true"
          android:sharedUserId="android.uid.system">

    <!-- Permiso de status bar -->
    <uses-permission android:name="android.permission.STATUS_BAR" />

    <!-- Permisos de configuración segura -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.DUMP" />

    <!-- Permisos de proceso y tareas -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.REAL_GET_TASKS" />
    <uses-permission android:name="android.permission.MANAGE_ACTIVITY_TASKS" />

    <!-- Permisos de overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_ALERT" />

    <!-- Permisos de rendimiento -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- Permisos de red -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Permisos Android 14+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Para lectura de estado del teléfono -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Permisos estándar -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />"
    <!-- ========== CONFIGURACIÓN DE LA APP ========== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.GameSpace"
        android:persistent="true"
        android:directBootAware="true"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:appCategory="game"
        android:debuggable="true"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute"
        tools:targetApi="34">

        <!-- ACTIVITY PRINCIPAL (TRANSPARENTE) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:finishOnTaskLaunch="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.GameSpace">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SERVICIO PRINCIPAL (PROCESO SEPARADO) -->
        <service
            android:name=".GameSpaceService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="systemExempted"
            android:isolatedProcess="false"
            android:process=":gamespace">
            <intent-filter>
                <action android:name="mx.xperience.gamespace.START" />
                <action android:name="mx.xperience.gamespace.STOP" />
            </intent-filter>
        </service>

        <!-- RECEIVER PARA AUTO-INICIO -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.REBOOT" />
            </intent-filter>
        </receiver>

        <!-- METADATAS DE SISTEMA -->
        <meta-data
            android:name="android.app.lib_name"
            android:value="" />
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

        <!-- PARA ANDROID 12+ -->
        <property
            android:name="android.app.PROPERTY_SUPPORT_PICTURE_IN_PICTURE"
            android:value="false" />

        </application>
    </manifest>
